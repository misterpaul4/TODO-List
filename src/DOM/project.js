const projectDom = () => {
/*	const container = document.createElement('div');
	const newProjectButton = document.createElement('button');
	const formEle = document.createElement('form');
	const titleEle = document.createElement('input');
	const descriptEle = document.createElement('textarea');

	newProjectButton.textContent = 'Add New Project';
	titleEle.setAttribute('type', 'text');
	titleEle.setAttribute('placeholder', 'TITLE');

	container.appendChild(newProjectButton);

	document.body.appendChild(container);
	document.body.appendChild(formEle);*/
};

export default projectDom